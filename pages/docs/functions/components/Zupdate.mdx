# Zupdate

Signals that the interaction response should update the original message instead of sending a new reply. Only valid inside interaction handlers (buttons, select menus).

## Syntax

```zbr
Zupdate{}
```




## Example

```zbr
Zupdate{}
```

